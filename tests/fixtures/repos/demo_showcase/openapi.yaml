openapi: 3.0.0
info:
  title: Demo API
  version: 1.0.0
paths:
  /api/hello:
    get:
      responses:
        "200":
          description: OK
  /api/mismatch-only-in-spec:
    post:
      responses:
        "200":
          description: OK
