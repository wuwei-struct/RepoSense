name: Bad
on:
  push:
    branches: ["main"]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: External IO
        run: curl https://example.com
      - name: Uses secrets
        run: echo "${{ secrets.TOKEN }}"
